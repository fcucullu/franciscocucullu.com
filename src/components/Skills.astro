---
---

<section data-fast-scroll="scroll_to_skills" class="box skills">
  <div class="stack gap-2 lg:gap-4">
    <div class="metric">100%</div>
    <h2>Interaction Coverage</h2>
    <p>Built an AI QA system architected to scale to 9M+ daily interactions and 20K+ users, live today across 51 accounts (9,400 users, 100% coverage, 82% accuracy) using multi-modal AI: audio, speech-to-text, and LLM-based transcript analysis.</p>
  </div>
  <div class="stack gap-2 lg:gap-4">
    <div class="metric">$$$</div>
    <h2>Multi-Million Savings</h2>
    <p>Driving multi-million dollar annual savings for a global CX leader through automated quality assurance and AI-powered insights.</p>
  </div>
  <div class="stack gap-2 lg:gap-4">
    <div class="metric">1 wk</div>
    <h2>Idea → MVP</h2>
    <p>Leading WASP (Weekly AI Sprint Protocol), taking AI products from ideation to working MVP in one week. 13 personal apps shipped solo.</p>
  </div>
</section>


<style>
	.box {
		border: 1px solid var(--gray-800);
		border-radius: 0.75rem;
		padding: 1.5rem;
		background-color: var(--gray-999_40);
		box-shadow: var(--shadow-sm);
	}

	.skills {
		display: flex;
		flex-direction: column;
		gap: 3rem;
	}

	.skills h2 {
		font-size: var(--text-lg);
	}

	.skills p {
		color: var(--gray-400);
	}

	.metric {
		font-size: 2.5rem;
		font-weight: 800;
		background: var(--gradient-accent-orange);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		line-height: 1;
	}

	@media (min-width: 50em) {
		.box {
			border-radius: 1.5rem;
			padding: 2.5rem;
		}

		.skills {
			display: grid;
			grid-template-columns: repeat(3, 1fr);
			gap: 5rem;
		}

		.skills h2 {
			font-size: var(--text-2xl);
		}

		.metric {
			font-size: 3rem;
		}
	}
</style>
